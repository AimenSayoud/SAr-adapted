# CLAUDE.md

InSAR analysis of the Rzecin floating peatland (Poland). Sentinel-1 C-band, 356
interferograms, 2022–2024. The result is a **negative one**: per-pixel inversion fails
physically, and the detectable seasonal signal is dielectric rather than mechanical.

## Commands

```bash
make test       # synthetic ground-truth test suite
make check      # manuscript numbers vs exported CSVs — run before any claim
make phases     # validate config/phases.yaml (the declared pipeline)
make readme     # regenerate the README phase table from phases.yaml
make docx       # check + regenerate Appendix B + assemble + pandoc
```

Heavy processing runs in Colab (`notebooks/phaseXX_*.ipynb`), reading data from the
Drive-mounted `insar_rzecin/`. The user's Mac has 2 cores and 8 GB: never propose running
MintPy, phase linking or null realisations locally.

## Numbers — the rule that matters most

**Never write a number into prose by hand.** Its source of truth is a CSV in
`docs/paper/figures/T*.csv`.

A referee found seven quantities where the text and Appendix B disagreed. Every one came
from a value being typed once and never revisited. `src/insar_wetlands/paper_numbers.py`
exists to prevent recurrence:

- `REGISTRY` maps each load-bearing number to its CSV, column and format.
- `SUPERSEDED` lists dead values (e.g. `−0.069`) that must never reappear.
- `make check` fails if a registered number is absent or a superseded one is present.

**When you introduce or change a load-bearing number, add it to `REGISTRY` in the same
change.** One line, permanent protection.

## Starting a phase

Every phase notebook opens the same way:

```python
from insar_wetlands.bootstrap import start
ctx = start("phaseG")
```

`start` mounts Drive, loads the config, resolves paths, configures logging to both the
notebook and `outputs/<phase>/run.log`, and returns a `Context`. Everything on it is lazy —
`ctx.pairs`, `ctx.template`, `ctx.dem`, `ctx.zones`, `ctx.layer(name)`, `ctx.to_grid(obj)` —
so a phase pays only for what it uses.

**Never rebuild that preamble by hand, and never construct a path from a literal.**
`paths.py` is the only place a path is made; a Colab-only default hidden in a signature is
what this replaced.

All 38 notebooks are migrated. Two keep their own setup — `build_manuscript_docx` and
`phaseK_referee_response` — because they do not call `load_config()` in the recognised pattern.

## Archiving a run

Every phase execution ends with one line, so the run survives the Colab session:

```python
from insar_wetlands.run_archive import archive_run
archive_run("phaseG", outdir, params=PARAMS, products=PRODUCTS)
```

It writes `<Drive>/runs/<phase>/<UTC timestamp>_<git sha>/manifest.json` recording the
commit, whether the tree was dirty, package versions, parameters and products. Runs are
never overwritten. Heavy files (`.nc`, `.tif`, `.h5`) are recorded by path, size and hash
rather than copied. `compare_runs("phaseG")` says what changed between the last two runs —
code, environment or parameters.

**Any new or re-run phase must call it.** A result whose environment was not captured
cannot be defended later.

## The pipeline is declared

`config/phases.yaml` holds all 38 phases: question, inputs, outputs, `status`
(current / superseded / exploratory / tooling) and the paper section each supports.

- **Adding a phase means adding an entry**, not inventing a new convention.
- **Never re-run a `superseded` phase**, and never make a live phase read from one —
  `make phases` fails on both.
- The README table is generated by `make readme`. Do not hand-edit it.

## Files you must not edit by hand

| File | Why |
|---|---|
| `docs/paper/09_appendix_data.md` | Generated from `figures/T*.csv` on every build |
| `docs/paper/_manuscript.md` | Generated by `assemble_markdown` from the numbered sections |
| `README.md` phase table | Generated by `make readme` from `config/phases.yaml` |

Edit the numbered section files (`00_title_abstract.md` … `08_references.md`) instead.

## Layout

- `src/insar_wetlands/` — all real code. Notebooks are thin callers, not logic.
- `docs/paper/` — English submission draft. **Authoritative.**
- `docs/article/` — French laboratory record. Detailed, superseded by `docs/paper/` for anything going out.
- `docs/paper/traceability.md` — every number → its source notebook.
- `tests/` — synthetic tests that recover a known ground truth.

## Testing

Every scientific routine has a test that recovers a known answer from simulated data
(EVD phase linking on a sparse network; aggregation recovering a displacement buried under
per-pixel noise; the collapse of a spurious correlation between two annual cycles). **Any
new scientific routine needs one before it is used in the paper.** A routine without a
ground-truth test is not evidence.

## Domain constraints

- **Do not propose a seventh inversion algorithm.** Six estimators with distinct
  mathematical assumptions fail identically; H1 is settled and the failure is physical.
- Empirical p-values have a floor of 1/(1+N). Always state it when reporting one at the floor.
- Nulls must be **size-matched** to the zone under test. Aggregate noise falls as 1/√N, so a
  larger null understates the floor and manufactures detections.
- Bog breathing is **seasonal**. Fitting a velocity to a periodic signal returns ≈ 0 by
  construction and has no power on the physics.
- The paper's claims are scoped to HyP3 products on this network. Phase linking here is not
  the maximum-likelihood estimator on a true SLC covariance matrix — do not restate it as one.

## Style

- Report what the data show, including refutations. Two pre-registered predictions of the
  authors' own were falsified; that is a feature of the paper and stays in.
- Prefer stating an assumption explicitly over quietly relying on it.
- Commit messages say **why**, not what: "Correct the seasonal p-value after the null was
  size-matched", not "update".
- Do not add dependencies without asking. The stack is numpy / pandas / scipy / xarray /
  scikit-learn and stays small.

## Open work

See `$HOME/Documents/Research_Hub/STATUS.md` for current versus superseded numbers and the
blocking list. `docs/paper/response_to_referees.md` records the round-2 corrections; the
July draft in the hub's `99_archive/` predates them and must not be used as a source.
